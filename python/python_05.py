# [getcwd() -> 현재 디렉토리 / chdir() -> 디렉토리를 이동시키는 함수]
# [listdir() -> 입력한 경로의 파일과 파일 목록을 리스트로 반환하는 함수]
# [os.path.exists() -> 특정 폴더가 있는지 확인하는 함수]

# [mkdir() -> 파일 만드는 함수 / makedirs() -> 하위 폴더도 만들어주는 함수 ex) "c : temp1 \\ temp2" ]
# [rmdir() / removedirs() -> 이건 하위 폴더까지 지워줌]
